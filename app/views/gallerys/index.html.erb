<%= paginate @gallerys %>

<% @gallerys.each do |gallery| %>
  <div class="booyah-box col-10 offset-1">
    <h1><%= link_to gallery.name, gallery_path(gallery) %></h1><br />
    <% if gallery.user.present? %>
      <small><%= gallery.user.email %></small>
    <% end %>
  </div>
<% end %>
